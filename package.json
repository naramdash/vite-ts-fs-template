{
  "name": "vite-fs-ts",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "dotnet tool restore",
    "dev": "dotnet fable watch src --run vite",
    "build": "tsc && vite build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.3.2",
    "vite": "^2.5.4"
  },
  "dependencies": {
    "tslib": "^2.3.1"
  }
}
